PORT=5000
MONGO_URI=mongodb://localhost:27017/realestate
JWT_SECRET=replace_with_a_long_random_string
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN_DAYS=30
CLIENT_ORIGIN=http://localhost:5173
# Multi-tenancy (resolveTenant middleware, see shared/middleware/resolveTenant.js)
PLATFORM_ROOT_DOMAIN=myplatform.com
DEFAULT_AGENCY_SLUG=dreamhomes
# AI Assistant (features/ai) runs fully offline - keyword/entity intent
# matching against MongoDB-backed tools (see ai/localEngine/). No API
# key or external provider required.
